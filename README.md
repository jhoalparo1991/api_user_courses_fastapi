# API USERS WITH FASTAPI

create virtualenv
`python -m venv venv`

activate virtualenv
`.\venv\Scripts\activate`

install packages
`pip install -r requirements.txt `


*Jhonatan Alberto Padilla Rovira*